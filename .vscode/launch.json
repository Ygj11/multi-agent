{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI Debug",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app"
                // "--reload"
            ],
            "cwd": "${workspaceFolder}/agent-development",  // ← 关键：切换到子目录
            "jinja": true,
            "console": "integratedTerminal"
        }
    ]
}