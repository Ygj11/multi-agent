# Q1.md

## 问题1

1. 意图识别节点的IntentResult结果，工具是限制死的？不是通过 LLM 自己判断需要哪些工具？
2. skills的定义不统一，比如troubleshooting的SKILL.md是一种写法，troubleshooting_agent下的各种SKILL.md是另一种写法，只需保留一种方式。
3. query_rewrite_node节点：
3.1 原始query没有命中第一轮规则，则进行第二轮的 _context_text。
3.2 _context_text方法目前是将recent_messages: list[dict[str, Any]]和short_summary: str拼接起来，然后使用规则判断的，这个recent_messages取的合理吗？为什么需要所有的最近消息？
3.3. 判断多轮追问，用FOLLOW_UP_MARKERS？有什么更加合理的方法？

4. intent_recognition_nodei节点：
4.1 意图识别也是靠if self._is_follow_up(original_query) and self._has_troubleshooting_context(context_text)，这种规则实现？有什么更好的方法？而且靠的是query个context_txt这种粗略的规则匹配，这完全不行吧？
4.2 意图识别子智能体，将使用的工具都给出了，这不就是相当于硬编码工具了吗？合理的应该根据子agent自我推理，使用何种工具吧？

5. knowledge_service
5.1 RAG的知识获取，目前是self.chunks = chunks or self._default_chunks(), 使用的是mock数据并且非语义相似度，而是基于关键词包含关系的规则分数。
5.2 后续优化：这个chunks是通过接口访问得到的一个对象，对象中有chunks，这个具体后续要怎么做？并且得到的chunks需要做排序top-k，这个使用什么相似度模型计算呢？依靠LLM自身的能力取可以吗？还是有什么更好的方法？

6. subAgent
6.1 子智能体的选择是怎么选择的？
6.2 子智能体的skills是怎么获取的？
6.3 tools是怎么依靠什么选择的？

7. LangGraph的节点没有配置话
7.1 所有的节点都是可配置的，可以形成ui界面的配置，托取哪几个状态点则会走哪几个节点

8. UI界面
8.1 日志可查询化，节点获取的信息，流转状态等



9. 主 Agent 任务级编排
- 举个例子：规划一次厦门之旅
- 规划与推理：智能体首先会将这个高层级目标分解为一系列逻辑子任务，例如：[确认出行偏好] -> [查询目的地信息] -> [制定行程草案] -> [预订票务住宿]。这是一个内在的、由模型驱动的规划过程。
10. 子 Agent 进行任务执行
- 工具使用：在执行规划时，智能体识别到信息缺口，会主动调用外部工具来补全。例如，它会调用天气查询接口获取实时天气，并基于“预报有雨”这一信息，在后续规划中倾向于推荐室内活动。
- 动态修正：在交互过程中，智能体会将用户的反馈（如“这家酒店超出预算”）视为新的约束，并据此调整后续的行动，重新搜索并推荐符合新要求的选项。整个“查天气 → 调行程 → 订酒店”的流程，展现了其根据上下文动态修正自身行为的能力。


11. 框架化
- 一个好的框架会提供一个通用的 Agent 基类或执行器，它封装了智能体运行的核心循环（Agent Loop）。无论是 ReAct 还是 Plan-and-Solve，都可以基于框架提供的标准组件快速搭建，从而避免重复劳动。
- 实现核心组件的解耦与可扩展性：一个健壮的智能体系统应该由多个松散耦合的模块组成。框架的设计会强制我们分离不同的关注点，比如：
- 模型层 (Model Layer)：负责与大语言模型交互，可以轻松替换不同的模型（OpenAI, Anthropic, 本地模型）。
- 工具层 (Tool Layer)：提供标准化的工具定义、注册和执行接口，添加新工具不会影响其他代码。
- 记忆层 (Memory Layer)：处理短期和长期记忆，可以根据需求切换不同的记忆策略（如滑动窗口、摘要记忆）。 这种模块化的设计使得整个系统极具可扩展性，更换或升级任何一个组件都变得简单。












# A
1. 实体抽取是在self.intent_recognition_node.recognize中，优化方案：
- 最好的方案是“规则/实体相关性先召回 + LLM 做最终判断/重排 + 规则兜底”。













## 程序启动，检查-agent_card_loader.validate_with_skill_catalog(skill_catalog)
1. 所有 AgentCard 能正常加载。
2. 所有 Skill metadata 能正常扫描。
3. 每个 Agent 至少有一个 enabled default skill。
4. AgentCard.skills 里声明的每个 skill_id 都真实存在。
5. Skill.agent 必须和 AgentCard.agent_name 匹配。
6. Skill.private_tools 不能超出 AgentCard.private_tools 授权范围。
7. 每个 Skill 引用的 agent 必须有对应 AgentCard。


## 存储方面
- MessageStore 保存完整聊天记录。
- ShortTermMemoryManager 保存压缩后的会话摘要。

## card_loader.py 中的 match_candidates，匹配子agent
- card 加分制，灵活配置
- query token 命中，判断 token 命中了多少（觉得有点幻）
- 