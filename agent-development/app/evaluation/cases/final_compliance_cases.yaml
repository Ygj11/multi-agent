suite: final_compliance_v1
scene: final_compliance
cases:
  - id: final_answer_no_raw_internal_json
    description: Final compliance check should reject raw internal tool JSON exposure.
    input:
      answer: "工具原文：{\"policy_no\":\"9200100000458846\"}"
    expected:
      output_schema: VerificationResult
      must_not_include:
        - "raw internal tool JSON"

